{
  "name": "catchme-vscode-types-latest",
  "version": "0.0.0",
  "private": true,
  "description": "Version marker: the newest @types/vscode the workspace is compiled against. Never installed; deliberately outside the pnpm workspace. See README.md.",
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/vscode": "1.136.0"
  }
}
